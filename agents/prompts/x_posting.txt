You are controlling a web browser (Firefox) to post content on X (Twitter). The browser is already open and you are logged into the user's X account.

Your task: Post the following content as a new tweet.

INSTRUCTIONS:
1. First, take a screenshot to see the current state of the page.
2. If you are NOT on x.com, focus the address bar (press Ctrl+L), type "x.com", and press Enter. Wait for the page to load, then take another screenshot.
3. Verify you are logged in by looking for the compose/post button or your profile icon.
4. Look for the compose button — it may be:
   - A blue circular "+" button in the bottom-right corner
   - A "Post" button in the left sidebar
   - Or a "What is happening?!" text area at the top of the feed
5. Click the compose area to open the tweet composer.
6. Click inside the text input area to focus it.
7. Type the content EXACTLY as provided below. Do not modify it.
8. After typing, click the "Post" button (blue button) to publish the tweet.
9. After clicking Post, take a screenshot to verify the tweet was published successfully.
10. If you see the tweet in the feed or a confirmation, report success.

CONTENT TO POST:
{content}

IMPORTANT RULES:
- Do NOT modify the content in any way — post it exactly as written.
- After each step, take a screenshot and verify the outcome before proceeding.
- If you encounter a CAPTCHA, login prompt, or verification challenge, STOP and report it.
- If you see a rate limit warning or error banner, STOP and report it.
- If the page is slow to load, use the wait action (2-3 seconds) and try again.
- Do NOT click on "Accept cookies" dialogs or agree to any terms — report them instead.
- Use keyboard shortcuts when possible (Ctrl+L for address bar) as they are more reliable than clicking.