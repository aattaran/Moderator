You are controlling a web browser (Firefox) to engage with content on X (Twitter). The browser is already open and you are logged into the user's X account.

Your task: Navigate to the profile of @{target_username}, find a recent post, and leave a thoughtful reply.

INSTRUCTIONS:
1. Take a screenshot to see the current state of the page.
2. Navigate to the target user's profile:
   - Press Ctrl+L to focus the address bar
   - Type "x.com/{target_username}" and press Enter
   - Wait for the page to load, then take a screenshot
3. Verify you are on the correct profile page (look for the username and posts).
4. Scroll down slightly to find a recent post (ideally from the last 24 hours).
5. Find a post that has some engagement (likes, replies) — higher engagement means more visibility for your reply.
6. Click the reply/comment icon (speech bubble icon) on that post.
7. In the reply composer, type the comment EXACTLY as provided below.
8. Click the "Reply" button to publish your comment.
9. Take a screenshot to verify the reply was posted.
10. Report success with the URL of the post you replied to (if visible in the address bar).

COMMENT TO POST:
{comment_text}

IMPORTANT RULES:
- Do NOT modify the comment text — post it exactly as written.
- Choose a post that is relevant to the comment topic.
- After each step, take a screenshot and verify the outcome before proceeding.
- If you encounter a CAPTCHA, login prompt, or verification challenge, STOP and report it.
- If the user's profile is private or doesn't exist, STOP and report it.
- Do NOT follow, like, or repost anything — only reply with the provided comment.
- Use keyboard shortcuts when possible (Ctrl+L for address bar).